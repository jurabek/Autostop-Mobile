<?xml version="1.0" encoding="UTF-8"?>
<ListView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Autostop.Client.Mobile.UI.Views.AddressesListView"
             SeparatorVisibility="None">
    <ListView.ItemTemplate>
        <DataTemplate>
            <ViewCell>
                <ViewCell.View>
                    <Grid ColumnSpacing="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image Source="marker.png" WidthRequest="20" Margin="5" HeightRequest="20" Grid.Column="0"/>
                        <StackLayout Grid.Column="1" VerticalOptions="Center" Margin="5" Spacing="0">
                            <Label Text="{Binding PrimaryText}" FontSize="12" TextColor="Black"/>
                            <Label Text="{Binding SecondaryText}" FontSize="10" TextColor="DimGray" LineBreakMode="TailTruncation"/>
                        </StackLayout>
                    </Grid>
                </ViewCell.View>
            </ViewCell>
        </DataTemplate>
    </ListView.ItemTemplate>
</ListView>